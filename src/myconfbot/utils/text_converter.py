import re

def simple_text_to_html(text):
    """
    Преобразует простой текст в HTML форматирование
    для обычных пользователей
    """
    # Заголовки (строка с ** в начале и конце)
    text = re.sub(r'\*\*(.+?)\*\*', r'<b>\1</b>', text)
    
    # Курсив (строка с _ в начале и конце)
    text = re.sub(r'_(.+?)_', r'<i>\1</i>', text)
    
    # Моноширинный (строка с ` в начале и конце)
    text = re.sub(r'`(.+?)`', r'<code>\1</code>', text)
    
    # Списки (строки начинающиеся с •, -, или *)
    text = re.sub(r'^[•\-*]\s+(.+)$', r'• \1', text, flags=re.MULTILINE)
    
    # Переносы строк
    text = text.replace('\n', '<br>')
    
    # Экранирование HTML символов
    text = text.replace('&', '&amp;')
    text = text.replace('<', '&lt;')
    text = text.replace('>', '&gt;')
    
    return text

def get_formatting_help():
    """Возвращает справку по форматированию для админа"""
    return """
🎨 *Как оформлять текст:*

*Жирный текст* → **оберните в двойные звездочки**
_Курсив_ → _оберните в одинарные подчеркивания_
`Код` → `оберните в обратные кавычки`

*Списки:* 
• Начинайте строки с точки
- Или с дефиса
* Или со звездочки

*Эмодзи:* просто вставляйте 🎂 📞 💼

*Пример:*
**Добро пожаловать!**
_Мы рады вас видеть_
• Торты
• Пирожные
• Конфеты
🎂 Вкуснейшие десерты!
"""